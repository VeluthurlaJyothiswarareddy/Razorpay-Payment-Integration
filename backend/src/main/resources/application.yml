server:
  port: 8080

spring:
  application:
    name: razorpay-integration
  data:
    mongodb:
      uri: mongodb://localhost:27017/razorpay_db

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_SwgANOuBns}
  key-secret: ${RAZORPAY_KEY_SECRET:67EacdegunfadcJP7r4F}
  webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:your_webhook_secret}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.razorpay.integration: DEBUG
